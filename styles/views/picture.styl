picture(
  $baseClass = "." + $viewPrefix + "Picture",
)
  {$baseClass}
    position: relative
    display: block

    > canvas
      position: absolute
      top: 0
      left: 0
      width: 100%
      height: 100%